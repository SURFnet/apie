# SPDX-FileCopyrightText: 2026 SURF B.V.
# SPDX-License-Identifier: EPL-2.0 WITH Classpath-exception-2.0

on:
  push: { tags: [ "*" ] }

jobs:
  build:
    uses: ./.github/workflows/job-build.yml
    secrets: inherit

  nvd:
    uses: ./.github/workflows/job-nvd.yml

  check:
    uses: ./.github/workflows/job-check.yml
